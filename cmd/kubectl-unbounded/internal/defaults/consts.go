package defaults

const (
	LabelKeyDefaultBootstrapTokenSecret   = "unbounded-kube.io/default-bootstrap-token"
	LabelValueDefaultBootstrapTokenSecret = "true"

	LabelKeyDefaultSSHSecret   = "unbounded-kube.io/default-ssh-secret"
	LabelValueDefaultSSHSecret = "true"

	// SSHSecretNamespace is the namespace where SSH secrets are stored.
	// This must match the namespace used by the machina controller
	// (SecretNamespaceMachinaSystem in cmd/machina).
	SSHSecretNamespace = "machina-system"

	// SSHSecretName is the default name of the SSH private key secret
	// generated by "kubectl unbounded setup".
	SSHSecretName = "machina-ssh"
)
