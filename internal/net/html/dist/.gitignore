# Keep this directory present in the repo as the embed root.
# `make net-frontend` writes the Vite build output here (index.html,
# assets/, .frontend-build-key); none of that is tracked.
*
!.gitignore
