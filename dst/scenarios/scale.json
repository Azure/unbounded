{
  "schema": 1,
  "nodes": 16,
  "known_failures": [
    {
      "suite": "scale-wave",
      "owner": "dataplane/runtime",
      "seed": 19,
      "rationale": "Observed HTTP fallback on warmed relay path [0,1,5,15]; the existing RDMA-only relay assertion fails. The runner must report failure, not accept fallback.",
      "reproducer": "python3 dst/run.py run --profile scale --scenario scale-wave --seed 19 --artifacts dst/artifacts/scale-wave-repro",
      "exit_criterion": "Explain and fix the relay fallback or establish an approved correction to the fixture contract, then pass the complete unchanged-strength scale gate."
    }
  ],
  "suites": [
    {"id": "scale-wave", "selector": "runtime::dst::active_1024_synchronous_wave_and_large_pairs", "tier": "scale", "exact": true, "ignored": true, "timeout_seconds": 180},
    {"id": "scale-recovery", "selector": "runtime::dst::scale_failure_recovery_acceptance", "tier": "scale", "exact": true, "ignored": true, "timeout_seconds": 180},
    {"id": "slab-format-rejection", "selector": "allocator::tests::incompatible_slab_versions_are_rejected_without_modification", "tier": "scale", "exact": true, "timeout_seconds": 30},
    {"id": "peer-format-bounds", "selector": "cache::metadata::peer_wire::algorithm_versioned_metadata_and_page_descriptors_are_exact_and_bounded", "tier": "scale", "exact": true, "timeout_seconds": 30}
  ]
}
