---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: gatewaypoolnodes.net.unbounded-kube.io
spec:
  group: net.unbounded-kube.io
  names:
    kind: GatewayPoolNode
    listKind: GatewayPoolNodeList
    plural: gatewaypoolnodes
    shortNames:
    - gpn
    singular: gatewaypoolnode
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.gatewayPool
      name: Gateway Pool
      type: string
    - jsonPath: .spec.nodeName
      name: Node
      type: string
    - jsonPath: .spec.site
      name: Site
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GatewayPoolNode represents route advertisements from a gateway
          pool node.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GatewayNodeSpec defines immutable identity metadata for a
              GatewayNode.
            properties:
              gatewayPool:
                description: GatewayPool is the owning gateway pool name.
                type: string
              nodeName:
                description: NodeName is the Kubernetes node name publishing this
                  GatewayNode.
                type: string
              site:
                description: Site is the site label of this gateway node.
                type: string
            required:
            - gatewayPool
            - nodeName
            type: object
          status:
            description: GatewayNodeStatus defines the advertised routes and lease
              heartbeat for a gateway node.
            properties:
              lastUpdated:
                description: LastUpdated is the heartbeat timestamp updated by the
                  node agent.
                format: date-time
                type: string
              routes:
                additionalProperties:
                  description: GatewayNodeRoute describes a single advertised route.
                  properties:
                    intermediateHops:
                      description: IntermediateHops identifies additional path objects
                        between source and destination.
                      items:
                        description: GatewayNodePathHop is a single path hop object.
                        properties:
                          name:
                            description: Name is the hop object name.
                            type: string
                          type:
                            description: 'Type is the hop object kind (for example:
                              Site, GatewayPool).'
                            type: string
                        required:
                        - name
                        - type
                        type: object
                      type: array
                    paths:
                      description: |-
                        Paths is a list of full end-to-end paths.
                        Each item is one complete ordered hop sequence from origin to local advertiser.
                      items:
                        items:
                          description: GatewayNodePathHop is a single path hop object.
                          properties:
                            name:
                              description: Name is the hop object name.
                              type: string
                            type:
                              description: 'Type is the hop object kind (for example:
                                Site, GatewayPool).'
                              type: string
                          required:
                          - name
                          - type
                          type: object
                        type: array
                      maxItems: 100
                      type: array
                    source:
                      description: Source identifies the originating object for this
                        route.
                      properties:
                        name:
                          description: Name is the hop object name.
                          type: string
                        type:
                          description: 'Type is the hop object kind (for example:
                            Site, GatewayPool).'
                          type: string
                      required:
                      - name
                      - type
                      type: object
                    type:
                      description: Type identifies the route source type (NodeCidr,
                        PodCidr, RoutedCidr).
                      type: string
                  required:
                  - type
                  type: object
                description: |-
                  Routes is a map of reachable CIDRs advertised by this gateway node.
                  The map key is the CIDR prefix.
                maxProperties: 1000
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
