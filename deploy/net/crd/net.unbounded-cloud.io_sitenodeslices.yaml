# Copyright (c) Microsoft Corporation.
# Licensed under the MIT License.
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: sitenodeslices.net.unbounded-cloud.io
spec:
  group: net.unbounded-cloud.io
  names:
    kind: SiteNodeSlice
    listKind: SiteNodeSliceList
    plural: sitenodeslices
    shortNames:
    - sns
    singular: sitenodeslice
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .siteName
      name: Site
      type: string
    - jsonPath: .sliceIndex
      name: Index
      type: integer
    - jsonPath: .nodeCount
      name: Nodes
      priority: 1
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SiteNodeSlice contains a slice of nodes belonging to a site
          Each slice can contain up to 500 nodes to limit object size
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          nodeCount:
            description: NodeCount is the number of nodes in this slice.
            type: integer
          nodes:
            description: Nodes contains detailed information about nodes in this slice
            items:
              description: NodeInfo contains detailed information about a node in
                a site
              properties:
                internalIPs:
                  description: InternalIPs are the node's internal IP addresses
                  items:
                    type: string
                  type: array
                name:
                  description: Name is the node name
                  type: string
                podCIDRs:
                  description: PodCIDRs are the pod CIDRs assigned to this node
                  items:
                    type: string
                  type: array
                wireGuardPublicKey:
                  description: WireGuardPublicKey is the node's WireGuard public key
                  type: string
              required:
              - name
              type: object
            type: array
          siteName:
            description: SiteName is the name of the Site this slice belongs to
            type: string
          sliceIndex:
            description: SliceIndex is the index of this slice (0-based)
            type: integer
        required:
        - siteName
        - sliceIndex
        type: object
    served: true
    storage: true
    subresources: {}
