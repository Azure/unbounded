# Copyright (c) Microsoft Corporation.
# Licensed under the MIT License.
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: azuremachines.infrastructure.unbounded-cloud.io
spec:
  group: infrastructure.unbounded-cloud.io
  names:
    kind: AzureMachine
    listKind: AzureMachineList
    plural: azuremachines
    shortNames:
    - azm
    singular: azuremachine
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.resourceID
      name: Resource ID
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AzureMachine contains the Azure provider state for one Unbounded
          Machine.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AzureMachineSpec defines the Azure resource owned by an Unbounded
              Machine.
            properties:
              resourceID:
                description: ResourceID is the full Azure Resource Manager ID of the
                  virtual machine.
                minLength: 1
                type: string
            required:
            - resourceID
            type: object
            x-kubernetes-validations:
            - message: resourceID is immutable
              rule: self.resourceID == oldSelf.resourceID
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
