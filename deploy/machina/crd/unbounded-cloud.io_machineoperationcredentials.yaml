# Copyright (c) Microsoft Corporation.
# Licensed under the MIT License.
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: machineoperationcredentials.unbounded-cloud.io
spec:
  group: unbounded-cloud.io
  names:
    kind: MachineOperationCredential
    listKind: MachineOperationCredentialList
    plural: machineoperationcredentials
    shortNames:
    - mocred
    singular: machineoperationcredential
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.siteName
      name: Site
      type: string
    - jsonPath: .spec.provider
      name: Provider
      type: string
    - jsonPath: .spec.auth.mode
      name: Auth Mode
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha3
    schema:
      openAPIV3Schema:
        description: |-
          MachineOperationCredential defines the credential source used by external
          MachineOperation controllers for machines in a site.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              MachineOperationCredentialSpec defines a provider credential source for a
              site.
            properties:
              auth:
                description: Auth identifies how the provider should authenticate.
                properties:
                  mode:
                    description: Mode identifies the credential source.
                    enum:
                    - WorkloadIdentity
                    - ExternalPlugin
                    type: string
                  secretRef:
                    description: |-
                      SecretRef references provider-specific external plugin configuration. It
                      is required for ExternalPlugin mode.
                    properties:
                      name:
                        description: Name of the secret.
                        minLength: 1
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        minLength: 1
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                required:
                - mode
                type: object
              provider:
                description: |-
                  Provider identifies the external control provider this credential applies
                  to.
                minLength: 1
                type: string
              siteName:
                description: SiteName is matched against the Machine site label.
                minLength: 1
                type: string
            required:
            - auth
            - provider
            - siteName
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
