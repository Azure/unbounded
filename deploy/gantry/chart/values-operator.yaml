image:
  reference: ""
  repository: gantry
  tag: operator-managed
  pullPolicy: IfNotPresent

namespace:
  create: true

manager: unbounded-operator

nodeConfig:
  enabled: false

overlaybdConfig:
  # Internal render switch. The operator mutator removes this workload unless
  # a Site explicitly enables Artifact Streaming.
  renderOnly: true
