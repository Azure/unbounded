# Strategic merge patch, not a standalone Kubernetes resource.
# Standalone only: render with --set RacerCacheName=gantry.
# Without a name this optional patch is empty. Operator-managed deployments use
# the ClusterCache gantry-backing annotation and generate the whole pod atomically.
{{ if .RacerCacheName -}}
{{ if not (regexMatch "^[a-z0-9]([a-z0-9-]{0,61}[a-z0-9])?$" .RacerCacheName) -}}
{{ fail "RacerCacheName must be the selected ClusterCache metadata.name (1..63 lowercase alphanumeric/hyphen characters, alphanumeric edges)" }}
{{ end -}}
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: gantry
  namespace: {{ default "unbounded-system" .Namespace }}
spec:
  template:
    metadata:
      annotations:
        unbounded-cloud.io/gantry-cache-name: {{ .RacerCacheName | quote }}
    spec:
      automountServiceAccountToken: false
      securityContext:
        supplementalGroups: [65532]
      initContainers:
        - name: chown-hostpaths
          command:
            - sh
            - -ec
            - |
              chown -R 65532:65532 /var/lib/gantry/libp2p
              chmod 0700 /var/lib/gantry/libp2p
              mkdir -p /run/racer/{{ .RacerCacheName }}/client /run/racer/{{ .RacerCacheName }}/origin
              chgrp 65532 /run/racer /run/racer/{{ .RacerCacheName }} /run/racer/{{ .RacerCacheName }}/client /run/racer/{{ .RacerCacheName }}/origin
              chmod 2770 /run/racer /run/racer/{{ .RacerCacheName }} /run/racer/{{ .RacerCacheName }}/client /run/racer/{{ .RacerCacheName }}/origin
          volumeMounts:
            - name: racer-sockets
              mountPath: /run/racer
      containers:
        - name: gantry
          args:
            - agent
            - --config=/etc/gantry/config.yaml
            - --content-backend=racer
            - --racer-cache-name={{ .RacerCacheName }}
          ports:
            - containerPort: 5001
              $patch: delete
            - containerPort: 5002
              $patch: delete
          volumeMounts:
            - name: racer-sockets
              mountPath: /run/racer
      volumes:
        - name: racer-sockets
          hostPath:
            path: /run/racer
            type: DirectoryOrCreate
{{ end -}}
