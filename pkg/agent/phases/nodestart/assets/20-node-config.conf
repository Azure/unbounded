# Managed by unbounded-agent. Do not edit directly.
[Service]
Environment="KUBELET_NODE_CONFIG_ARGS=--node-labels={{.NodeLabels}} --register-with-taints={{.RegisterWithTaints}} --client-ca-file={{.ClientCAFile}} --cluster-dns={{.ClusterDNS}}"
