# Managed by unbounded-agent. Do not edit directly.
[Service]
{{- if .UseExecCredential}}
Environment="KUBELET_KUBECONFIG_ARGS=--kubeconfig={{.KubeconfigPath}}"
{{- else}}
Environment="KUBELET_KUBECONFIG_ARGS=--bootstrap-kubeconfig={{.BootstrapKubeconfigPath}} --kubeconfig={{.KubeconfigPath}}"
{{- end}}
